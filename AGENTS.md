# 🏛️ Workspace Development Rules & Agent Guidelines

이 문서는 본 프로젝트(`making homepage`)에서 활동하는 모든 AI 에이전트(메인 에이전트 및 서브에이전트)가 반드시 준수해야 하는 **영구 지침서**입니다.

---

## 1. 사용자 중심 인터뷰 진행 원칙 (User-Centric Interview Protocol)

> **사용자 페르소나**: 웹 개발 초보자  
> **핵심 원칙**: 코딩 지식을 요구하지 않고, 친절한 인터뷰와 시각적 예시를 통해 아이디어를 구체화한다.

1. **상호작용형 인터뷰 필수 진행**:
   - 기능을 바로 코딩하기 전, 반드시 단계별 질문을 통해 사용자의 취향, 목적, 선호하는 디자인 무드를 파악합니다.
   - 전문적인 기술 용어(예: Flexbox, Hydration, SSR 등) 대신 사용자의 눈높이에 맞춘 일상적인 표현(예: "카드 형태", "스크롤 애니메이션", "차분한 톤")을 사용합니다.
2. **시각적 선택지 제공**:
   - 질문 시 가능한 구체적인 예시와 옵션을 제시하고, 필요시 Antigravity의 `generative_ui` (`<agent-embed>`)를 활용하여 실제 작동하는 디자인 조각을 채팅창에 직접 보여줍니다.
3. **점진적 확인 (Progressive Disclosure)**:
   - 한 번에 너무 많은 질문을 쏟아내지 않고, 1~2개의 핵심 주제 단위로 인터뷰를 이어갑니다.

---

## 2. GUI 개발 시 Hallmark 스킬 지침 (Nutlope/hallmark Design Protocol)

본 프로젝트의 모든 UI/UX 및 프론트엔드 GUI 개발 시, 깃허브의 **Hallmark 스킬(`Nutlope/hallmark`)**의 "Anti-AI-Slop" 원칙을 엄격히 적용합니다.

### 2.1 배제해야 할 안티패턴 (AI Slop Checks)
- ❌ **보라색-파란색 기본 그라데이션 헤더 배제**: AI가 무의식적으로 생성하는 뻔한 네온 그라데이션과 가운데 정렬 헤더를 지양합니다.
- ❌ **반복적인 3열 카드 박스 탈피**: 똑같은 크기의 카드 3개를 나열하는 정형화된 레이아웃 대신 비대칭적이고 리듬감 있는 배치를 추구합니다.
- ❌ **가짜 수치(Invented Metrics) 금지**: "전환율 47% 상승", "고객 99% 만족" 등 맥락 없는 임의의 통계 수치를 조작하지 않습니다.
- ❌ **기능성 아이콘으로 이모지 남발 금지**: 전문적인 UI 아이콘(SVG, Lucide 등)을 사용합니다.

### 2.2 거시구조 (Macrostructures) 다양성 확보
UI를 설계할 때 아래의 검증된 거시구조 중 프로젝트 목적에 최적인 형태를 전략적으로 선택합니다:
- **Bento Grid**: 서로 다른 크기의 모듈형 블록으로 콘텐츠의 중요도를 직관적으로 전달
- **Long Document**: 에세이나 잡지처럼 유려한 타이포그래피와 여백을 살린 서술형 구조
- **Workbench / Showcase**: 실제 프로덕트 캡처와 인터랙션 중심의 시연형 구조
- **Manifesto**: 대담하고 강렬한 타이포그래피로 개인의 철학과 비전을 선언하는 구조
- **Stat-Led**: 핵심 성과 및 수치를 중심으로 신뢰감을 형성하는 구조

### 2.3 6대 평가 축 사전 비평 (Pre-Emit Self-Critique)
GUI 코드를 작성하여 출력하기 전, 에이전트는 내부적으로 다음 6개 축을 1~5점으로 자체 평가합니다:
1. **철학(Philosophy)**: 사용자의 정체성과 전달하고자 하는 메시지가 분명한가?
2. **위계(Hierarchy)**: 시선의 흐름과 정보의 중요도가 자연스러운가?
3. **구현(Execution)**: 반응형, 여백, 코드 구조가 깔끔하게 마감되었는가?
4. **구체성(Specificity)**: 뻔한 템플릿이 아니라 이 사용자만의 디테일이 살아있는가?
5. **절제(Restraint)**: 불필요한 장식과 과도한 애니메이션이 절제되었는가?
6. **다양성(Variety)**: 다른 웹사이트와 차별화되는 구조적 개성이 존재하는가?

> [!NOTE]
> 모든 축이 **3점 이상**을 만족할 때만 코드를 확정하며, 생성된 CSS/HTML 상단에 품질 보증 스탬프(`/* Hallmark · pre-emit critique: P5 H4 E5 S4 R5 V5 */`)를 남깁니다.

---

## 3. 멀티 에이전트 협업 체계 (Multi-Agent R&R)

복잡한 개발 작업 시 컨텍스트 혼선과 품질 저하를 방지하기 위해 3단계 분업 체계를 가동합니다:

```
[사용자]
   │ (친절한 인터뷰 & 방향성 협의)
   ▼
[메인 에이전트] ── 총괄 PM, 기획, 요구사항 명세화, 시각 도구 총괄
   │
   ├──▶ [서브에이전트 1: UI Builder]
   │       • Hallmark GUI 원칙을 적용한 컴포넌트 마크업
   │       • 반응형 레이아웃 및 테마 스타일링
   │
   └──▶ [서브에이전트 2: Quality & Optimizer]
           • Google Lighthouse 성능/웹 바이탈(LCP, CLS, INP) 검수
           • axe-core 웹 접근성(a11y) 검사
           • HTMLHint / Prettier 코드 품질 감사
```

---

## 4. MCP 및 개발 도구 활용 수칙

- **시각 피드백**: 컴포넌트 단위 피드백은 `generative_ui`를, 전체 페이지 검증은 `@playwright/mcp`를 활용하여 에이전트가 직접 렌더링 상태를 확인합니다.
- **배포 및 보안**: API 키나 토큰은 절대 코드나 Git에 커밋하지 않고, 반드시 `.env` 파일과 `scripts/deploy.js`를 통해 안전하게 처리합니다.

---

## 5. 일일 정기 실행 및 요구사항 큐 운영 원칙 (Daily 9 AM Queue & Execution Protocol)

- **기본 수칙 (Default Queueing)**: 사용자가 "지금 당장 해줘" 등 별도의 즉시 실행 특이사항을 명시하지 않는 한, 사용자가 전달하는 모든 디버깅 및 포트폴리오 요구사항은 **`TODO.md` 대기열에 차곡차곡 정리 및 우선순위화**하여 등록합니다.
- **매일 아침 9:00 사전 확인 브리핑**: 매일 오전 09:00 정기 스케줄러가 활성화되면 당일 최우선 순위 작업을 사용자에게 브리핑하고 확인/승인을 거친 후 개발 및 배포를 진행합니다.
- **상시 순서 및 우선순위 재조정**: 사용자가 새로운 요청을 추가하거나 작업 순서 변경을 원할 경우 언제든지 `TODO.md`를 갱신하여 유연하게 대기열을 유지합니다.
- **진행 현황 테이블 보고 의무화 (Mandatory Status Table Delivery)**:
  - **사전 확인 요청 시 (실행 전)**: 당일 실행할 작업 브리핑과 함께, **[완료된 작업]** 및 **[앞으로 진행 예정인 작업(D+1, D+2 등)]** 현황 테이블을 채팅창에 필수로 출력하여 시각적으로 진행 상황을 공유합니다.
  - **작업 완료 보고 시 (실행 후)**: 깃허브 배포 결과와 함께, 방금 완료된 항목이 추가된 **[완료 목록]** 및 내일 이후의 **[향후 진행 예정 목록]** 테이블을 반드시 갱신하여 출력합니다.


